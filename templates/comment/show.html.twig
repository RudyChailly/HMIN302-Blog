<div id="comment-{{ comment.id }}" class="border border-primary">
    <p> {{ comment.content }} </p>
    <div> De <a href="{{ path('user_show', {'username': comment.author.username}) }}">{{ comment.author.username }} </a>  le {{ comment.published|date("d/m/Y") }} à {{ comment.published|date("H:i") }}</div>

    <a href="{{ path('comment_edit', {'id': comment.id}) }}">edit</a>
    <button class="reportCommentButton" id="commentReportButton-{{ comment.id }}">report</button>
    {{ include('comment/_delete_form.html.twig') }}
</div>
