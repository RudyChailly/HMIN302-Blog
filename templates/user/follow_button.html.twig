{% if (formFollowButton is defined) and (app.user is not null) and (user.id != app.user.id) %}
    {{ form_start(formFollowButton) }}
    {% if user in app.user.follows %}
        {{ form_widget(formFollowButton.follows, { 'attr': {'class': 'btn-pill btn-primary user-show-button-unfollow btn-action-user'}, 'label': ' ' }) }}
    {% else %}
        {{ form_widget(formFollowButton.follows, { 'attr': {'class': 'btn-pill btn-outline-primary btn-action-user'}, 'label': 'S\'abonner' }) }}
    {% endif %}
    {{ form_end(formFollowButton) }}
{% endif %}



