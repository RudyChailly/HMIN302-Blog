{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}

    <div class="container mt-3">
        <div class="row justify-content-center">
            <div class="bg-white p-4 shadow position-relative">
                <h1 class="text-center"> Edition de profil </h1>
                {{ include('user/_form.html.twig', {'action': 'update'}) }}
                {{ include('user/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
