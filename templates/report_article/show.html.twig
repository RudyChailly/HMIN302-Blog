{% extends 'base.html.twig' %}

{% block title %}ReportArticle{% endblock %}

{% block body %}
    <h1>ReportArticle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ report_article.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ report_article.category }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ report_article.description }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ report_article.created ? report_article.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('report_article_index') }}">back to list</a>

    <a href="{{ path('report_article_edit', {'id': report_article.id}) }}">edit</a>

    {{ include('report_article/_delete_form.html.twig') }}
{% endblock %}
